fastapi>=0.127.0
uvicorn[standard]>=0.40.0

# ORM (Pydantic + SQLAlchemy)
sqlmodel>=0.0.31

# Postgres driver (SQLAlchemy "postgresql+psycopg://")
psycopg[binary]>=3.3.2

# Settings
pydantic-settings>=2.12.0

# Auth
bcrypt>=5.0.0
python-jose[cryptography]>=3.5.0
python-multipart>=0.0.21
pyotp>=2.9.0

# Redis for pub/sub and rate limiting
redis>=5.2.0

# Rate limiting (FastAPI middleware, Redis backend)
slowapi>=0.1.9

# Payment processing

# Image processing
Pillow>=12.1.0

# HTTP requests for wine import
requests>=2.32.5

# Email sending
aiosmtplib>=3.0.0

# Contact validation (registration, reservations)
email-validator>=2.2.0
phonenumbers>=8.13.50

# PDF generation
reportlab>=4.4.7

# Excel export for reports
openpyxl>=3.1.0

# Tests (FastAPI TestClient requires httpx; pytest for discovery in CI/container)
httpx>=0.28.0
pytest>=8.3.0
