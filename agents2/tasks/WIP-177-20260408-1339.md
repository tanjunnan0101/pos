# WIP-Task: Enhance Enlace de reseña en Google

## GitHub Issue
- **Number:** `#177`
- **Title:** Enhance Enlace de reseña en Google
- **URL:** https://github.com/satisfecho/pos/issues/177
- **Labels:** agent:wip (test **FAIL** — manual `/settings` blocked by `POST /api/token` **500**; see latest **Test report**)
- **Created:** 

## Meta
- **Status:** wip
- **Generated:** `20260408-1339`

---

### Task: Enhance Enlace de reseña en Google

**Priority:** Implement as described in GitHub issue

### Issue Summary
Inside "Settings" (/settings):
1. Translate SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION and SETTINGS.PUBLIC_GOOGLE_REVIEW_INSTRUCTIONS using i18n files and Angular translate.
2. Ensure PUBLIC_GOOGLE_REVIEW_DESCRIPTION and PUBLIC_GOOGLE_REVIEW_INSTRUCTIONS appear below "Enlace de reseña en Google".

### Steps
1. [x] Identify where "Enlace de reseña en Google" is rendered in the settings component/template.
2. [x] Find the translation keys used for these placeholders in the backend/database or config.
3. [x] Add translation entries to `front/public/i18n/*.json` (all shipped locales).
4. [x] Update the Angular template to use `translate` pipe for these descriptions.
5. [x] Reorder elements in the template so descriptions are below the label (shown when the review URL field is empty).
6. [ ] Verify with smoke tests/manual check (tester) — **blocked:** `/api/token` 500 in this environment.

### Files
- **Must:** 
    - `front/src/app/settings/settings.component.ts` (inline template: Google review block under Contact)
    - `front/public/i18n/*.json` (Translation files)

- **Consider:** `back/...` (if keys are coming from backend settings) — not required; strings are i18n keys.

### Criteria
- [x] SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION is translated via i18n.
- [x] SETTINGS.PUBLIC_GOOGLE_REVIEW_INSTRUCTIONS is translated via i18n.
- [x] Descriptions are positioned below "Enlace de reseña en Google" (label), in the same form group; helper copy appears when the field is empty.

### Testing
- [ ] Manual: Check `/settings` in browser (ensure translation works) — **not completed** (login 500).
- [x] Automated: optional landing smoke (`LANDING_VERSION_ONLY=1`) — pass.

### Notes
- Template: `settings.component.ts` — `PUBLIC_GOOGLE_REVIEW_URL` label, input, then `@if (publicGoogleReviewUrlEmpty())` block with two `<p>` lines using the translate pipe for DESCRIPTION and INSTRUCTIONS.

### References
- Issue: https://github.com/satisfecho/pos/issues/177

---

| Phase | Status |
|-------|--------|
| Created | ✅ |
| Review | ✅ |
| Development | ✅ |
| Testing | ❌ **FAIL** (2026-04-13 11:24 UTC): manual `/settings` blocked — `POST /api/token` **500**; `docker compose` unusable (overlay2 I/O error). Coder: fix `/api/token` / DB auth or re-queue **UNTESTED** after local login works. |

---

## Testing instructions

1. **Environment:** Local dev with stack up (e.g. HAProxy on `http://127.0.0.1:4202`). Log in as a user who can open **Settings**.
2. **Navigation:** Open **Settings** → **Contact info** tab (`/settings`, contact section).
3. **Google review link:** Clear the **Google review link** field if it has a value so the explanatory paragraphs show.
4. **Expect:** Below the field, two paragraphs appear: description text and Place ID instructions, both translated per current UI language (try **es**, **en**, **de** at minimum).
5. **Optional quick smoke:** From `front/`, `BASE_URL=http://127.0.0.1:4202 npm run test:landing-version` to confirm the app still loads after i18n changes.

---

## Test report

1. **Date/time (UTC) and log window:** 2026-04-13 (session); verification against live app ~12:00–12:30 UTC window. Container logs not collected: `docker compose ps` failed on this host with daemon overlay I/O error; no `pos-front` / `pos-back` log scrape available.

2. **Environment:** `BASE_URL=http://127.0.0.1:4202` (HAProxy responds HTTP 200 on `/` and `/api/health` returns `{"status":"ok"}`). Branch `development` @ `257d9f0`. Compose: intended `docker-compose.yml` + `docker-compose.dev.yml` (not verified via `docker compose` due to Docker daemon error).

3. **What was tested:** Per **Testing instructions**: (i) optional landing smoke; (ii) i18n assets for **es** / **en** / **de** for `SETTINGS.PUBLIC_GOOGLE_REVIEW_*`; (iii) attempt staff login to reach `/settings` and exercise Contact → Google review field; (iv) static verification of `settings.component.ts` template order vs criteria.

4. **Results (criteria):**
   - **i18n keys present and valid JSON (es, en, de):** **PASS** — `python3 -m json.tool` on `front/public/i18n/es.json`, `en.json`, `de.json` exit 0.
   - **Shipped i18n bundles expose DESCRIPTION / INSTRUCTIONS in browser (es, en, de):** **PASS** — In-page `fetch('/i18n/es.json'|'en.json'|'de.json')` shows `SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION` and `SETTINGS.PUBLIC_GOOGLE_REVIEW_INSTRUCTIONS`; ES `PUBLIC_GOOGLE_REVIEW_URL` value is `Enlace de reseña en Google`.
   - **Template: label → input → empty-state paragraphs:** **PASS** — `front/src/app/settings/settings.component.ts`: label `public_google_review_url`, input, then `@if (publicGoogleReviewUrlEmpty())` with two `<p>` using translate pipe (lines ~865–878 in current tree).
   - **Manual: `/settings` Contact tab, clear field, verify copy in es / en / de:** **FAIL** — **Blocked:** `POST http://127.0.0.1:4202/api/token?tenant_id=1` returns **HTTP 500** (`Internal Server Error`) for demo credentials from repo `.env`; Puppeteer `test:landing-version` also fails at login with 500. Cannot reach authenticated Settings UI in this environment.
   - **Optional: `npm run test:landing-version` (full, with login):** **FAIL** — same login 500.
   - **Optional: landing-only smoke (`LANDING_VERSION_ONLY=1`):** **PASS** — footer shows package version string.

5. **Overall:** **FAIL** — Failed criterion: live **Settings** verification and full landing+login smoke (authentication endpoint error).

6. **Product owner feedback:** The Google review helper strings are present in **es**, **en**, and **de** JSON and load in the browser; the settings template places translated description paragraphs under the field when the URL is empty. End-to-end confirmation on **Settings** was not possible here because **login returns 500**; restore API/auth (or local DB/session health) and re-run the manual steps, or run tests on a host where `/api/token` succeeds.

7. **URLs tested (numbered):**
   1. `http://127.0.0.1:4202/`
   2. `http://127.0.0.1:4202/login?tenant=1`
   3. `http://127.0.0.1:4202/api/health`
   4. `http://127.0.0.1:4202/i18n/es.json` (via `fetch` in page context)
   5. `http://127.0.0.1:4202/i18n/en.json` (via `fetch` in page context)
   6. `http://127.0.0.1:4202/i18n/de.json` (via `fetch` in page context)
   7. `POST http://127.0.0.1:4202/api/token?tenant_id=1` (login attempt — 500)

8. **Relevant log excerpts:** N/A — Docker CLI unusable (overlay I/O error). Evidence: `curl`/`fetch` results above; sample: `GET /api/health` → 200 `{"status":"ok"}`; `POST /api/token` → 500 `Internal Server Error`.

---

## Test report (retest — 2026-04-13)

1. **Date/time (UTC) and log window:** Start **2026-04-13 11:06 UTC**; probes through **~11:10 UTC**. `docker compose … ps` still fails (`open …/overlay2/…/lower: input/output error`); no `pos-front` / `pos-back` log window collected.

2. **Environment:** `BASE_URL=http://127.0.0.1:4202`. Branch **`development`** @ **`7c2e02a`**. Compose files not confirmed via CLI (daemon error). `GET /` → **200**; `GET /api/health` → **200** `{"status":"ok"}`.

3. **What was tested:** Same as **Testing instructions**: static i18n + template; `POST /api/token` for staff login; optional `LANDING_VERSION_ONLY=1` landing smoke.

4. **Results (criteria):**
   - **i18n JSON valid (es, en, de):** **PASS** — `python3 -m json.tool` on each file exits 0.
   - **Template: label → input → `@if (publicGoogleReviewUrlEmpty())` → two translated `<p>`:** **PASS** — `settings.component.ts` lines 864–878 (`label` / `input` / `@if` block with `SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION` and `SETTINGS.PUBLIC_GOOGLE_REVIEW_INSTRUCTIONS`).
   - **Manual `/settings` with es / en / de:** **FAIL** — `POST http://127.0.0.1:4202/api/token?tenant_id=1` returns **500** with demo credentials from `.env` (password not logged); cannot open Settings.
   - **Optional `LANDING_VERSION_ONLY=1 npm run test:landing-version`:** **PASS** — script reports landing version **2.0.73** and commit **13b60e1** in footer text.

5. **Overall:** **FAIL** — Required manual authenticated **Settings** verification still blocked by **login 500**.

6. **Product owner feedback:** Code and translations for the Google review helper block look correct by inspection and JSON validation; the running stack on this host still cannot authenticate for UI testing. Fix **`/api/token`** (backend/DB/config) or re-run verification where login works, then rename task back to **UNTESTED** for the tester.

7. **URLs tested (numbered):**
   1. `http://127.0.0.1:4202/`
   2. `http://127.0.0.1:4202/api/health`
   3. `POST http://127.0.0.1:4202/api/token?tenant_id=1` (500)

8. **Relevant log excerpts:** N/A (Docker CLI I/O error). Evidence: `curl` — health **200**; token endpoint **500** body `Internal Server Error`.

---

## Test report (retest — 2026-04-13)

1. **Date/time (UTC) and log window:** Start **2026-04-13 11:15 UTC**; probes **11:15–11:16 UTC**. `docker compose ps` and `docker logs pos-back` **not available** — daemon returns `input/output error` on overlay2 paths; no container log window.

2. **Environment:** `BASE_URL=http://127.0.0.1:4202`. Branch **`development`** @ **`cbbf009`**. Compose: `docker-compose.yml` + `docker-compose.dev.yml` (not verified via CLI — Docker daemon unhealthy). `GET /` → **200**; `GET /api/health` → **200** `{"status":"ok"}`.

3. **What was tested:** Per **Testing instructions**: (i) optional `LANDING_VERSION_ONLY=1` landing smoke; (ii) JSON validity and presence of `SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION` / `INSTRUCTIONS` in **es**, **en**, **de**; (iii) static template order in `settings.component.ts`; (iv) `POST /api/token?tenant_id=1` with repo `.env` demo credentials to reach `/settings`.

4. **Results (criteria):**
   - **i18n JSON valid; keys present (es, en, de):** **PASS** — `python3 -m json.tool` on each locale; Python assert on nested `SETTINGS` keys.
   - **Template: label → input → empty-state translated paragraphs:** **PASS** — `settings.component.ts` lines 864–878: `PUBLIC_GOOGLE_REVIEW_URL` label, input, `@if (publicGoogleReviewUrlEmpty())` with `SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION` and `SETTINGS.PUBLIC_GOOGLE_REVIEW_INSTRUCTIONS` in `form-description`.
   - **Manual: `/settings` Contact tab, clear Google review URL, verify copy in es / en / de:** **FAIL** — `POST http://127.0.0.1:4202/api/token?tenant_id=1` returns **HTTP 500** (`Internal Server Error`) with demo credentials; authenticated Settings UI not reachable.
   - **Optional `LANDING_VERSION_ONLY=1 npm run test:landing-version`:** **PASS** — landing footer shows version **2.0.73** and commit **13b60e1**.

5. **Overall:** **FAIL** — Required manual verification of **Settings** (per testing instructions) remains blocked by **`/api/token` 500**; Docker CLI on this host cannot supply backend logs for the failure.

6. **Product owner feedback:** Static checks confirm translations and template structure for the Google review helper block. Until staff login succeeds locally (fix backend/DB/env for **token** endpoint, or verify on a healthy stack), the UI acceptance criterion for this issue cannot be marked complete from this environment.

7. **URLs tested (numbered):**
   1. `http://127.0.0.1:4202/`
   2. `http://127.0.0.1:4202/api/health`
   3. `POST http://127.0.0.1:4202/api/token?tenant_id=1` (500)

8. **Relevant log excerpts:** **N/A** — Docker daemon I/O error prevents `docker compose` / `docker logs`. Evidence: `curl` health **200**; `POST /api/token` → **500** body `Internal Server Error`.

---

## Test report (2026-04-13 — tester run)

1. **Date/time (UTC) and log window:** Start **2026-04-13 11:24 UTC**; probes **11:24–11:26 UTC**. `docker compose -f docker-compose.yml -f docker-compose.dev.yml ps` fails: `open …/overlay2/…/lower: input/output error`; no `pos-front` / `pos-back` log scrape.

2. **Environment:** `BASE_URL=http://127.0.0.1:4202` (HAProxy responds). Branch **`development`** @ **`24d12ff`** (local tree). Compose files intended: `docker-compose.yml` + `docker-compose.dev.yml` (not verified via Docker CLI — daemon unhealthy). `GET /` → **200**; `GET /api/health` → **200** `{"status":"ok"}`.

3. **What was tested:** Per **Testing instructions**: (i) optional `LANDING_VERSION_ONLY=1 npm run test:landing-version`; (ii) JSON validity for **es** / **en** / **de**; (iii) static template order in `settings.component.ts`; (iv) `POST /api/token?tenant_id=1` with repo **`.env`** `DEMO_LOGIN_*` (password: `%20` decoded to space for form encode) to reach authenticated **Settings**.

4. **Results (criteria):**
   - **i18n JSON valid (es, en, de):** **PASS** — `python3 -m json.tool` on each file exits 0.
   - **Keys `SETTINGS.PUBLIC_GOOGLE_REVIEW_DESCRIPTION` / `INSTRUCTIONS` present (es, en, de):** **PASS** — verified via `grep` in `front/public/i18n/`.
   - **Template: label → input → `@if (publicGoogleReviewUrlEmpty())` → two translated `<p>`:** **PASS** — `settings.component.ts` ~865–877: `PUBLIC_GOOGLE_REVIEW_URL` label, input, `@if` block with `DESCRIPTION` and `INSTRUCTIONS` translate pipes.
   - **Manual: `/settings` Contact tab, clear Google review URL, verify copy in es / en / de:** **FAIL** — `POST http://127.0.0.1:4202/api/token?tenant_id=1` returns **HTTP 500** (`Internal Server Error`) with `DEMO_LOGIN_*` credentials; authenticated Settings UI not reachable.
   - **Optional `LANDING_VERSION_ONLY=1 npm run test:landing-version`:** **PASS** — script reports landing version **2.0.73** and footer commit **13b60e1**.

5. **Overall:** **FAIL** — Required manual **Settings** verification blocked by **`/api/token` 500**; cannot confirm UI in browser on this host.

6. **Product owner feedback:** Translations and template wiring for the Google review helper block remain consistent with the issue by static review. The running stack here still fails at login; repair the token endpoint / database user alignment (or run verification where Docker and login work), then return the task to **UNTESTED** for another tester pass.

7. **URLs tested (numbered):**
   1. `http://127.0.0.1:4202/`
   2. `http://127.0.0.1:4202/api/health`
   3. `POST http://127.0.0.1:4202/api/token?tenant_id=1` (**500**)

8. **Relevant log excerpts:** **N/A** — Docker daemon I/O error. Evidence: `curl` — `GET /api/health` → 200; `POST /api/token` → 500 body `Internal Server Error`.
