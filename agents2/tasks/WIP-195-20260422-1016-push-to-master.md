# Push to Master

## GitHub Issues
- **Issue:** https://github.com/satisfecho/pos/issues/195
- **195**

## Problem / goal
Promote tested work from **`development`** to **`master`** so production (e.g. **amvara9**) can run current code. After deploy, confirm the **GitHub Actions** deployment workflow completed successfully (per issue text).

Follow repo branching rules: routine promotion timing vs urgent production fixes — see **`.cursor/rules/git-development-branch-workflow.mdc`**.

## High-level instructions for coder
- Confirm **`development`** is synced with **`origin/development`** and reflects the intended release scope (no accidental WIP).
- Merge **`development` → `master`** only when allowed by team rules (cadence, production-impacting trigger, or explicit urgent/production request on the issue). Align with **`docs/agent-loop.md`**.
- Push **`master`** to **`origin`** when merging for production.
- Deploy to **amvara9** using the documented path (see **`docs/0001-ci-cd-amvara9.md`**, **`scripts/deploy-amvara9.sh`** as referenced in **`README.md`** / **`AGENTS.md`**).
- For “review the success status of the deployment action on GitHub”: open the repo’s **Actions** tab for the relevant workflow run and verify success; capture any failure logs for follow-up without pasting secrets.
- Post a short summary on the issue when promotion/deploy is done (what was merged, whether CI/deploy is green).

## Implementation summary (feature coder)
- **`development`** was synced with **`origin/development`** (already up to date).
- **`master`** was fast-forwarded to **`7a2c2bd`** (same tip as **`development`** after promotion) and pushed to **`origin/master`** (issue explicitly requested production promotion).
- **GitHub Actions — Deploy to amvara9** run **`24773000757`** (triggered by **`master`** push) **failed** at step **Fetch marketing site artifacts**: sync script reported **no `MARKETING_ARTIFACT_TOKEN` / `GH_TOKEN`** in that job’s environment, could not download marketing bundles, and **`MARKETING_VERIFY_NO_PLACEHOLDERS=1`** failed with placeholder bundles for slugs **antillana**, **dilruba**, **flamanapolitana**, **gustazo**, **hakone**. Server deploy steps did not run.
- **Follow-up (repo settings):** ensure Actions secrets include a PAT with **Actions read** on every repo listed in **`config/marketing-sites.json`** (see error text in workflow logs), then re-run the failed workflow or redeploy.

## Testing instructions

1. **Git:** Confirm **`origin/master`** matches **`origin/development`** at **`7a2c2bd`** (or later if additional commits landed):
   `git fetch origin && git rev-parse origin/master origin/development`
2. **GitHub Actions:** Open **Actions** → **Deploy to amvara9** → run **`24773000757`** (or latest **`master`** deploy). After secrets are fixed, either **Re-run failed jobs** or trigger a new deploy from **`master`** and expect **green** through **Fetch marketing site artifacts**, **Set up SSH**, **Build and restart stack on amvara9**, **Smoke test**.
3. **Optional live check:** After a **green** deploy, verify **`https://satisfecho.de/`** (or documented prod URL) and API health per **`docs/0001-ci-cd-amvara9.md`** / smoke step output.
4. **Manual fallback:** If CI cannot be fixed immediately, an operator may run **`scripts/deploy-amvara9.sh`** from the server checkout per **`README.md`** / **`AGENTS.md`** (still needs marketing bundles resolved for full parity with CI).

---

## Test report

1. **Date/time (UTC)** and log window: **2026-04-26 15:48:44 UTC**; task renamed **UNTESTED-** → **TESTING-**; verification used **`./scripts/git-sync-development.sh`**, repo **`git fetch`**, **`gh run view` / `gh run list`**, **`curl`**; no **`docker logs`** (outcome is CI/HTTP, not a local stack run).

2. **Environment:** Repo **pos** root; branch **development** local synced; **remote:** `origin/master` **`7a2c2bd59b2cfb6cbc6a55ac407993494b17256f`**, `origin/development` **`faf3a7b517927db23324feb937e6b95af1433be9`**; **`gh`** to **`satisfecho/pos`**. **BASE_URL (smoke):** `https://satisfecho.de`.

3. **What was tested:** **Testing instructions** §1–4: **`git rev-parse`**, **Deploy to amvara9** for run **24773000757** and list of latest **master** runs, optional prod HTTP, manual fallback not run.

4. **Results:**
   - **Git (objective):** **PASS (scope)** — **`origin/master`** is still **`7a2c2bd`**, matching the promotion in the **Implementation summary**; **`origin/development`** has **moved past** that tip (**`faf3a7b`**); inequality **development ≠ master** is **expected** after more commits on **development** (per instruction “or later if additional commits landed” the check is: master at promoted SHA; develop may diverge). Evidence: `git rev-parse origin/master origin/development` (two SHAs as above).
   - **GitHub Actions (full pipeline for issue #195 / marketing + amvara9):** **FAIL** — run **`https://github.com/satisfecho/pos/actions/runs/24773000757`** remains **completed failure**; **`X Fetch marketing site artifacts (curl + GitHub API)`**; **Set up SSH** through **Smoke test** are **skipped** (not run). `gh run list` **--workflow "Deploy to amvara9" --limit 8`: latest **master**-triggered run is still **24773000757** (failure, 2026-04-22); no newer **success** on **master** after that push in the sample.
   - **Optional live check (after green deploy):** **N/A** as acceptance gate (CI not green). Independent smoke: **`curl`** `https://satisfecho.de/` → **200**; `https://satisfecho.de/api/health` → **200**, body **`{"status":"ok"}`** (proves current prod; does **not** assert **7a2c2bd** or full deploy via this workflow).
   - **Manual server fallback:** Not run by this tester; blocked on same marketing/secret path as CI per task notes.

5. **Overall:** **FAIL** (primary criterion: **GitHub “Deploy to amvara9”** for the **master** push is **not green**; marketing fetch step still fails; deploy/smoke did not run).

6. **Product owner feedback:** Nothing has changed versus the last report: **production** responds, but the **recorded** **Actions** run for the **master** promote is still **red**. Unblock with **Actions** secrets / PAT for marketing artifacts (or successful **Re-run** after fix), then re-test. **Development** has new commits; future promotion may need a **fresh** merge to **master** once CI is fixable.

7. **URLs tested (numbered list):** **1** `https://satisfecho.de/` **2** `https://satisfecho.de/api/health` **3** `https://github.com/satisfecho/pos/actions/runs/24773000757` **4** (CLI) `git rev-parse` remotes; **5** (CLI) `gh run list` for workflow **Deploy to amvara9** — not browser for (4)–(5).

8. **Relevant log excerpts (last section):** **`gh run view 24773000757`**: `X master Deploy to amvara9`; jobs show **`X deploy`** · **`X Fetch marketing site artifacts`**, **`Set up SSH`** and below **unrun**; **`Process completed with exit code 1`**. **`gh run list`**: `completed failure` … `24773000757` … `2026-04-22T10:18:20Z` (latest **master** failure in list). **`curl`:** landing **200**, health **200** — `{"status":"ok"}`.
