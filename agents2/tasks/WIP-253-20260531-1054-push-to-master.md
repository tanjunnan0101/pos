# Push to master

## GitHub Issues
- **Issue:** https://github.com/satisfecho/pos/issues/253
- **253**
- **Supersedes:** https://github.com/satisfecho/pos/issues/252 (closed 2026-05-29; archived as **`CLOSED-252-20260529-1430-push-to-master.md`**)

## Problem / goal

Promote tested work from **`development`** to **`master`** so production (e.g. **amvara9**) can run current code, then confirm **Deploy to amvara9** completed successfully on GitHub Actions.

The issue author asks to merge **`development` → `master`** when appropriate and, if deploying to amvara9, verify the deployment workflow succeeded.

Follow **`.cursor/rules/git-development-branch-workflow.mdc`** for merge timing (production promotion / explicit push-to-master request).

## Current state (2026-05-31)

| Check | Value |
|-------|-------|
| **`origin/development`** | **`21692dd5`** (tip at 001 run) |
| **`origin/master`** | **`4fa55922`** (partial promotion; La Moca marketing-site registration) |
| Commits **`master..development`** | **894** |
| Latest **Deploy to amvara9** on **`master`** | **`26630264861`** — **failure** (workflow_dispatch, 2026-05-29) |
| Prior failure pattern | **Fetch marketing site artifacts** — likely missing **`MARKETING_ARTIFACT_TOKEN` / `GH_TOKEN`** in Actions per **`config/marketing-sites.json`** |

## High-level instructions for coder

- Sync **`development`**: **`./scripts/git-sync-development.sh`**.
- Merge **`development` → `master`** and push **`origin/master`** when allowed for this production-promotion scope.
- Ensure GitHub Actions secrets include a PAT with **Actions read** on repos listed in **`config/marketing-sites.json`**, then trigger or re-run **Deploy to amvara9** on **`master`**.
- Verify green deploy: **`gh run list --workflow "Deploy to amvara9" --branch master`** and **`gh run view <run-id>`**.
- Post a short summary on **#253** (merged SHA, deploy run id, green/fail).
- See **`docs/0001-ci-cd-amvara9.md`**, **`scripts/deploy-amvara9.sh`**, **`README.md`**.

## Testing instructions

1. **Git:** After promotion, **`git rev-parse origin/master origin/development`** — tips should match (or **`master`** is the intended release SHA).
2. **GitHub Actions:** Latest **Deploy to amvara9** for **`headBranch: master`** is **green** (marketing artifacts, SSH, build/restart, smoke test).
3. **Optional live:** **`https://satisfecho.de/api/health`** returns OK after deploy.
4. **Manual fallback:** Document if **`scripts/deploy-amvara9.sh`** was used instead of a green CI run.

**Pass criteria:** **`development`** promoted to **`master`** and **Deploy to amvara9** **green** for that commit (or documented manual parity).
